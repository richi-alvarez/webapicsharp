version: '3.9'

services:
  frontend:
    build:
      context: .           # Ruta donde está tu proyecto Blazor
      dockerfile: DockerfileDebug  # Debe ir dentro de build
    container_name: frontend
    ports:
      - "5106:5106"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
    networks:
      - local-network
  

networks:
  local-network:
    driver: bridge